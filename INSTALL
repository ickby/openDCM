Compiling Instructions
**********************

You need cmake to install this Application, check your
distributions repository or www.cmake.com for 
newest versions.

When installed, open a terminal in the source 
folder and type:

cmake -G"Unix Makefiles"

(with appropriate generator for your Operating System, 
see "cmake --help"). Then Type make (or use the generated
projektfiles for your IDE). Everything should compile fine.
When missing dependencies cmake will inform you, install the 
neaded librarys (dev-package) and try again.

